# Notion Export Configuration

# Notion API Key (必填)
# 从 https://www.notion.so/my-integrations 获取
NOTION_API_KEY=

# Notion Page ID (必填)
# 页面URL中的ID部分
# 注意：需要提取最后32位字符
NOTION_PAGE_ID=

# 输出文件名 (可选，默认为 exported_notion_page.md)
OUTPUT_FILENAME=notion_export.md

# 输出目录 (可选，默认为 exports)
OUTPUT_DIR=exports

# 是否递归导出子页面 (可选，默认为 false)
# 设置为 true 将导出所有链接的子页面
RECURSIVE=true

# 最大递归深度 (可选，默认为 0 表示无限制)
# 设置为正整数可以限制递归深度，避免过多页面导出
MAX_DEPTH=3

# 是否使用页面标题作为文件名 (可选，默认为 false)
# 设置为 true 将使用页面标题作为文件名，而不是页面ID
USE_PAGE_TITLE_AS_FILENAME=true
