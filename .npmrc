# 设置为严格模式，确保依赖版本一致性
strict-peer-dependencies=true

# 使用精确版本以保持一致性
save-exact=true

# 自动安装对等依赖
auto-install-peers=true

# 设置使用workspaces，支持monorepo结构
shamefully-hoist=true

# 设置node_modules位置
node-linker=hoisted

# 禁用progress bar，减少CI环境输出
progress=false
